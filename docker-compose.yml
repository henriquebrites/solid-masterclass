services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: solid_masterclass
    ports:
      - "5433:5432"
    volumes:
      - ./.data-postgres:/var/lib/postgresql/data

volumes:
  pgdata:

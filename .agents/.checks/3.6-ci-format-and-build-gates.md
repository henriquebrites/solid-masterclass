# 3.6 — CI format and build gates

Sources:

- `.agents/.tasks/3.6-ci-format-and-build-gates.md` - the three criteria, the Observable table, the Swept table

## Out of scope

- Smoke test (`node dist/index.js` actually boots) - Unresolved #1 in the task, needs extra DB infra in the pipeline

## Landing

Touches `package.json` (new script) and `.github/workflows/ci.yml` (two new steps). Reuses the existing `build` script (`tsc`, already emits per `tsconfig.json` outDir/rootDir) and the existing `prettier` devDependency - no new tooling installed.

| One-way door | Literal shape | Alternative rejected |
| --- | --- | --- |
| New `.prettierignore` excludes `.agents/`, `.claude/`, `drizzle/`, `pnpm-lock.yaml` | Prettier's default ignore-path already reads `.gitignore` (confirmed: `node_modules`/`dist`/`coverage` are silently skipped today); this file adds the paths that are tracked but not meant to be hand-formatted | reformatting all 85 currently-misformatted files including `.agents/` skill docs and `.harness-eval/` run reports - rejected because those are generated/operational artifacts skills keep writing without running Prettier, so `format:check` would go red again on the next unrelated PR that touches nothing under `src/` |

- Nothing else in this change is hard to reverse - removing the two CI steps or the script undoes it

## Checks

### S1 - Format check and build gates in CI · 9 files · 32.5 KB · ~8k

**C1** - `package.json` contains `"format:check": "prettier --check ."`
Proof: `node -e "if(require('./package.json').scripts['format:check']!=='prettier --check .')process.exit(1)"`

**C2a** - Against a correctly formatted, compilable commit, `pnpm run format:check` exits 0
Proof: `pnpm run format:check` (run at HEAD after the reformat in this slice)

**C2b** - Against the same commit, `pnpm run build` exits 0
Proof: `pnpm run build`

**C2c** - `.github/workflows/ci.yml` has a "Format check" step running `pnpm run format:check` and a "Build" step running `pnpm run build`
Proof: `grep -A1 'name: Format check' .github/workflows/ci.yml | grep -q 'run: pnpm run format:check'` and `grep -A1 'name: Build' .github/workflows/ci.yml | grep -q 'run: pnpm run build'`

**C2d** - The existing steps (Type-check, Lint, Apply database migrations, Run tests) keep their relative order
Proof: `awk '/name: Type-check/{t=NR} /name: Lint/{l=NR} /name: Apply database migrations/{m=NR} /name: Run tests/{r=NR} END{exit !(t<l && l<m && m<r)}' .github/workflows/ci.yml`

**C3** - A deliberately misformatted or non-compiling change fails the corresponding CI step
Proof: manual — exercised once in review against a throwaway commit for each step (one with a Prettier violation, one with an extension-less import per 3.1), never merged. No automated proof: the task itself specifies this is checked by exercising the step in review, not by shipping the broken commit.

## Swept

- validation: n/a (per source)
- failure modes: C3 - format/build failures are now CI failure modes
- idempotency and retry: n/a (per source) - CI's retry behavior is unchanged
- authorization: n/a (per source)
- concurrency and ordering: C2d - steps stay sequential in the same relative order
- data lifecycle: n/a (per source)
- external-dependency failure: n/a (per source) - no new external dependency
- state transitions: n/a (per source)
- observability: existing (per source) - plain CI step logs, no new instrumentation

## Coverage

- No enumerated set in this slice (no provider maps, no status enums, no startup-config assemblies) - the Observable and Swept tables above already list every row the task itself declared, and each is closed by a check or marked n/a/existing per source.
- Claims naming a status/exit code: C2a, C2b, C2c each has a proof that runs the real command and checks its exit code / the file content directly.

## Handoff

Single slice, ~8k tokens - no handoff, one agent builds and verifies the whole feature.

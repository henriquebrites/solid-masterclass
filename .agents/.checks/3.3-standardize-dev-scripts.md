# 3.3 — Padronizar scripts de desenvolvimento (typecheck e test:run)

Sources:

- `.agents/.tasks/3.3-standardize-dev-scripts.md` - settles intent, critérios, boundary, unresolved
- `package.json:8` - script `test` atual (`vitest`, watch mode)
- `.github/workflows/ci.yml:48,57` - comandos ad hoc `pnpm exec tsc --noEmit` e `pnpm exec vitest run`
- `.husky/pre-push:3` - comando ad hoc `pnpm exec vitest run`

## Out of scope

- Qualquer mudança de comportamento em `pnpm test` (continua watch mode, inalterado)
- Qualquer outro script ad hoc em CI ou hooks fora de type-check e testes (lint, migrations, etc.)

## Landing

Toca `package.json` (2 scripts novos), `.github/workflows/ci.yml` (2 steps) e `.husky/pre-push` (1 linha). Nenhum outro arquivo é afetado.

| One-way door | Literal shape | Alternative rejected |
| --- | --- | --- |
| None - renomeação/organização de comandos já existentes, reversível, sem schema ou contrato novo | | |

## Checks

### S1 - Scripts nomeados substituem comandos ad hoc · 3 files · ~0.5k

**C1** - `package.json` contém os scripts `"typecheck": "tsc --noEmit"` e `"test:run": "vitest run"`
Proof: `node -e "const pkg=require('./package.json'); if (pkg.scripts.typecheck !== 'tsc --noEmit' || pkg.scripts['test:run'] !== 'vitest run') process.exit(1)"` — exit code `0`

**C2** - `.github/workflows/ci.yml` invoca `pnpm run typecheck` e `pnpm run test:run` nos steps de type-check e testes, não mais `pnpm exec tsc --noEmit`/`pnpm exec vitest run`
Proof: `grep -q 'pnpm run typecheck' .github/workflows/ci.yml && grep -q 'pnpm run test:run' .github/workflows/ci.yml && ! grep -q 'pnpm exec tsc --noEmit' .github/workflows/ci.yml && ! grep -q 'pnpm exec vitest run' .github/workflows/ci.yml` — exit code `0`

**C3** - `.husky/pre-push` invoca `pnpm run test:run`, não mais `pnpm exec vitest run`
Proof: `grep -q 'pnpm run test:run' .husky/pre-push && ! grep -q 'pnpm exec vitest run' .husky/pre-push` — exit code `0`

**C4** - `pnpm test` sem argumentos continua iniciando o Vitest em modo watch, inalterado
Proof: `node -e "const pkg=require('./package.json'); if (pkg.scripts.test !== 'vitest') process.exit(1)"` — exit code `0`

## Swept

- validation: n/a
- failure modes: n/a - inalterado; falhas de tsc/vitest seguem o comportamento existente
- idempotency and retry: n/a
- authorization: n/a
- concurrency and ordering: n/a
- data lifecycle: n/a
- external-dependency failure: n/a
- state transitions: n/a
- observability: existing - reporters padrão do tsc/vitest inalterados

## Coverage

`profile: light` (nenhuma declaração de profile em `AGENTS.md`) - sem join de Coverage; C1-C4 usam uma asserção localizada cada.

## Handoff

Slice único, ~0.5k tokens estimados de leitura — muito abaixo do orçamento de 150k. Sem handoff: um agente conclui a task inteira em um único batch.

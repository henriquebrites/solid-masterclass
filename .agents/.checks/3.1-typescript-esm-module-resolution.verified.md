# 3.1 — TypeScript/ESM Module Resolution Verification

**Verdict**: PASS
**Profile**: light (no profile declared in AGENTS.md for tlc-implement, so the default applies)
**Diff range**: `cefacf0..HEAD` (single commit `2196a59` "fix: align TypeScript module resolution with the real tsc+node pipeline")
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

## Omissions (per light profile)

- **Step 1 (binding-source check)**: skipped — `ui`-only.
- **Step 4 (fault injection)**: skipped — `standard`/`ui`-only.
- **Coverage join / Test-policy judgment**: skipped — `standard`/`ui`-only. The checklist carries no `## Test policy` section, so this would have been an n/a line even under a heavier profile.
- **Binding sources table**: omitted from this report (no step-1 run to populate it).

Step 2 (run every proof) and step 3 (check the assertion, not its presence) ran in full. The `Swept` row resolving to `existing` was read against the code per instructions.

## Checks

| Check | Claim | Proof run | Evidence | Result |
|---|---|---|---|---|
| C1 | With `tsconfig.json` on `NodeNext`/`NodeNext` and relative imports still unextended, `pnpm run build` fails with TS2835 | `pnpm run build` in a scratch worktree (`git worktree add /tmp/claude-501/scratch-3.1-c1 cefacf0`) with only `tsconfig.json`'s `module`/`moduleResolution` edited to `NodeNext` (imports left as in `cefacf0`) | Command exited `1`; output includes e.g. `src/application/usecases/CreateUser.ts(3,37): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean '../../resources/repositories/UserRepository.js'?` and ten further TS2835/TS2834 lines across `src/application/factories/index.ts`, `src/drivers/app.ts`, `src/index.ts`, `src/resources/daos/UserDAO.ts`, `src/resources/repositories/UserRepository.ts` — exactly the 6 files the `Landing` names, minus the already-correct `CreateUser.ts` import that also triggered | PASS |
| C2 | After every relative import in `src/**/*.ts` gets an explicit `.js` extension, `pnpm run build` completes exit 0 with zero `tsc` errors | `pnpm run build` at HEAD (real tree, `git status --porcelain` clean before and after) | `$ tsc` with no further output, shell reported `EXIT:0` | PASS |
| C3 | `node ./dist/index.js`, run right after a C2-satisfying build, does not throw `ERR_MODULE_NOT_FOUND`/`ERR_UNSUPPORTED_DIR_IMPORT` before reaching the external-dependency boundary | `rm -rf dist && pnpm run build && node ./dist/index.js` at HEAD, backgrounded and observed for 3s then killed | Captured stdout: `Documentation running at http://localhost:4949/docs`. No `ERR_MODULE_NOT_FOUND` or `ERR_UNSUPPORTED_DIR_IMPORT` string anywhere in the output; process was still alive (had to be killed) after 3s, i.e. it ran past module resolution and into the Fastify startup / DB-connection phase | PASS |

## Swept row (existing) — checked against code

- `observability: existing - saída padrão do tsc/node já expõe a falha, nenhum log novo é adicionado`
  Verified: `git diff cefacf0..HEAD -- 'src/**/*.ts' | grep -nE '^\+.*console\.|^\+.*log\(|^\+.*logger'` returned no matches — no new logging/console statements were added anywhere in the `src/**/*.ts` diff. Confirmed.

## Other observations (not findings)

- The diff also reformats several lines in `src/drivers/app.ts`, `src/application/usecases/CreateUser.ts`, `src/resources/daos/UserDAO.ts`, `src/resources/repositories/UserRepository.ts` (line-wrapping/whitespace only, e.g. collapsing multi-line method chains, trailing-newline additions) beyond the strict "add `.js` extension" scope the `Landing` describes. These do not change any token other than whitespace/formatting — confirmed by re-reading the hunks — and do not affect execution, consistent with the `Landing`'s "Nenhum código de execução muda" claim. Not raised as a finding.

## Gate

`pnpm run build` (HEAD) — exit 0, 0 tsc errors.
`node ./dist/index.js` (HEAD, 3s observation) — no ERR_MODULE_NOT_FOUND / ERR_UNSUPPORTED_DIR_IMPORT, startup log observed.
`pnpm run build` (scratch worktree at `cefacf0` + NodeNext-only tsconfig edit) — exit 1, TS2835 present.
Scratch worktree removed; real tree porcelain confirmed clean/unchanged throughout.

# 3.7 — Final quality validation and AGENTS.md update

Sources:

- `.agents/.tasks/3.7-final-quality-validation.md` - the six criteria, the Observable table, the Swept table, the note that 3.1-3.6 must all be done first (confirmed: `.agents/.checks/3.1..3.6` all have a `.verified.md`)

## Out of scope

- Triggering the real GitHub Actions run over `.github/workflows/ci.yml` (C2) - the user decided this is proven by running, locally and in sequence, the exact command each CI step runs, rather than by pushing the branch (push needs explicit go-ahead per `AGENTS.md` and the skill's blast-radius rule, and was declined for this task)

## Landing

No source touched except `AGENTS.md` (four new script names added to the existing "Comandos de validação" list). No code, no config, no CI file changes - this task validates what 3.1-3.6 already shipped.

- Nothing in this change is hard to reverse - it is a documentation edit plus running existing commands

## Checks

### S1 - All quality commands green in sequence, CI-equivalent, hooks unchanged, dist boots, docs updated, UserDAO untouched · 3 files · ~40 KB · ~12k

**C1** - `pnpm run lint`, `pnpm run format:check`, `pnpm run typecheck`, `pnpm run build`, `pnpm run test:run` and `pnpm run test:coverage` each exit 0 when run in that sequence
Proof: `pnpm run lint && pnpm run format:check && pnpm run typecheck && pnpm run build && pnpm run test:run && pnpm run test:coverage` (exit code of the whole chain)

**C2** - Every `.github/workflows/ci.yml` step's literal command exits 0 when run locally in the file's order, against the local Postgres on `DATABASE_URL` from `.env.example`
Proof: `pnpm run typecheck && pnpm exec eslint . && pnpm run format:check && pnpm run build && pnpm exec drizzle-kit migrate --config=drizzle.config.ts && pnpm run test:run` (exit code of the whole chain, run against the local Postgres container)

**C3** - `.husky/pre-commit` runs `lint-staged` and `.husky/pre-push` runs `pnpm run test:run`, unchanged from their configured content
Proof: `grep -q 'pnpm exec lint-staged' .husky/pre-commit && grep -q 'pnpm run test:run' .husky/pre-push` and a real test commit/push exercises them once during Build

**C4** - `node ./dist/index.js`, started from the build produced by C1, does not throw a module-resolution error
Proof: `pnpm run build && timeout 5 node ./dist/index.js; test $? -ne 1` combined with inspecting the process output for `ERR_MODULE_NOT_FOUND` / `Cannot find module` (absence of either string in stdout/stderr before the timeout)

**C5** - `AGENTS.md`'s "Comandos de validação" section lists `typecheck`, `test:run`, `test:coverage` and `format:check` alongside the existing entries
Proof: `grep -c 'pnpm run typecheck\|pnpm run test:run\|pnpm run test:coverage\|pnpm run format:check' AGENTS.md` returns 4 (one occurrence each, located in the "Comandos de validação" section)

**C6** - `src/resources/daos/UserDAO.ts` and `UserDAO.test.ts` are unmodified since 3.1's verification commit, and `UserDAO.test.ts` still passes
Proof: `git log --oneline e31bcde..HEAD -- src/resources/daos/UserDAO.ts src/resources/daos/UserDAO.test.ts` (empty output) and `pnpm exec vitest run src/resources/daos/UserDAO.test.ts`

## Swept

- validation: n/a (per source)
- failure modes: n/a (per source)
- idempotency and retry: n/a (per source)
- authorization: n/a (per source)
- concurrency and ordering: n/a (per source)
- data lifecycle: n/a (per source)
- external-dependency failure: n/a (per source)
- state transitions: n/a (per source)
- observability: n/a (per source)

## Coverage

`profile: light` (no profile declaration in `AGENTS.md`) - no Coverage join. C1 and C2 each name their full literal command chain and are proven by the chain's own exit code, so there is no enumerated set behind them that a check could under-sample. C5 checks all 4 named scripts by an exact count, C6 covers both named files directly.

## Handoff

Single slice, ~12k tokens - no handoff, one agent builds and verifies the whole feature.

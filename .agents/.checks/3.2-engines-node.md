# 3.2 — Adicionar engines.node ao package.json

Sources:

- `.agents/.tasks/3.2-engines-node.md` - settles intent, critério, boundary, unresolved
- `.nvmrc:1` - versão mínima de Node fixada (`24`)
- `package.json` - manifesto atual, sem campo `engines`

## Out of scope

- `engine-strict=true` em `.npmrc` (bloqueio rígido de instalação) - Unresolved 1 da task fonte resolve para "sem enforcement rígido", apenas documentação via `engines`
- Qualquer valor de `engines.node` diferente de `">=24"` (ex. `"24.x"`) - Unresolved 1 assume faixa aberta na ausência de resposta

## Landing

Toca apenas `package.json` (adição de uma chave). Nenhum outro arquivo é afetado.

| One-way door | Literal shape | Alternative rejected |
| --- | --- | --- |
| None - mudança é só documentação da versão mínima de Node, totalmente reversível (remover o campo `engines`) | | |

## Checks

### S1 - `package.json` declara a versão mínima de Node · 1 file · ~0.1k

**C1** - `package.json` contém `"engines": { "node": ">=24" }`
Proof: `node -e "const pkg=require('./package.json'); if (pkg.engines?.node !== '>=24') process.exit(1)"` — exit code `0`

## Swept

- validation: n/a - não há entrada de usuário
- failure modes: n/a - nenhum modo de falha novo introduzido pelo campo `engines` em si
- idempotency and retry: n/a
- authorization: n/a
- concurrency and ordering: n/a
- data lifecycle: n/a
- external-dependency failure: n/a
- state transitions: n/a
- observability: existing - `pnpm` já emite aviso padrão quando `engines` diverge da versão ativa (sem `engine-strict`, é apenas aviso, não bloqueio)

## Handoff

Slice único, <1k tokens estimados de leitura — muito abaixo do orçamento de 150k. Sem handoff: um agente conclui a task inteira em um único batch.

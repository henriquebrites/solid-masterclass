# 2.2 — Documentar organização e compartilhamento das skills

Sources:

- https://linear.app/henrique-brites/issue/DEV-8/22-documentar-organizacao-e-compartilhamento-das-skills - descrição, critérios de aceite, arquivos envolvidos e prioridade

## Out of scope

- Mover, renomear ou converter qualquer arquivo em `.agents/skills/` ou `.claude/skills/` - a estrutura já funciona, a tarefa é apenas documental
- Editar `.agents/.skill-lock.json` - apenas referenciar
- Reorganizar a tabela de diretórios em `AGENTS.md` (assunto de outra tarefa, DEV-7) - esta tarefa edita `CLAUDE.md`

## Landing

Toca apenas `CLAUDE.md` (raiz), reutilizando o texto já existente sobre `.agents/skills/` como fonte e adicionando a explicação do mecanismo de symlinks. Nenhuma mudança de código ou de estrutura de diretórios.

- Nenhum door nesta mudança - é uma edição de texto em um arquivo de documentação, sem impacto em schema, contrato ou dado persistido

## Checks

### S1 - Documentar o mecanismo de symlinks em CLAUDE.md · 1 arquivo · ~1 KB · ~1k

**C1** - `CLAUDE.md` declara explicitamente que `.agents/skills/` é a fonte única de skills e que `.claude/skills/` contém apenas symlinks para essa fonte, nunca cópias
Proof: `grep -qi "fonte única" CLAUDE.md && grep -qi "symlink" CLAUDE.md && grep -qi "nunca" CLAUDE.md`

**C2** - `CLAUDE.md` instrui que uma nova skill deve ser adicionada em `.agents/skills/<nome>/SKILL.md` e depois exposta via symlink em `.claude/skills/<nome>`
Proof: `grep -q '\.agents/skills/<nome>/SKILL\.md' CLAUDE.md && grep -q '\.claude/skills/<nome>' CLAUDE.md`

**C3** - `CLAUDE.md` lista as 9 skills atualmente presentes (`commit-message`, `github-readme-generator`, `harness-eval`, `the-judge`, `tlc-discover`, `tlc-implement`, `tlc-plan`, `tlc-spec-driven`, `tlc-spec-lean`)
Proof: `for s in commit-message github-readme-generator harness-eval the-judge tlc-discover tlc-implement tlc-plan tlc-spec-driven tlc-spec-lean; do grep -q "$s" CLAUDE.md || echo "MISSING: $s"; done` (sem saída = todas presentes)

**C4** - Nenhum arquivo em `.agents/skills/` ou `.claude/skills/` foi movido, renomeado ou convertido de symlink para cópia
Proof: `git status --porcelain .agents/skills .claude/skills` (sem saída = nenhuma mudança) e `find .claude/skills -maxdepth 1 -type l | wc -l` retorna `9`

## Swept

- validation: não se aplica - mudança é texto de documentação, sem entrada de usuário
- failure modes: não se aplica - não há comportamento de runtime
- idempotency / retry: não se aplica - edição de arquivo estático
- authorization: não se aplica
- concurrency / ordering: não se aplica
- data lifecycle: não se aplica - nenhum dado persistido
- external-dependency failure: não se aplica
- state transitions: não se aplica
- observability: não se aplica

## Coverage

`profile: light` (nenhuma declaração de profile em `AGENTS.md`) - sem join de Coverage; C1-C3 usam uma asserção localizada cada, C4 confirma que o conjunto de 9 symlinks permanece intacto por contagem direta.

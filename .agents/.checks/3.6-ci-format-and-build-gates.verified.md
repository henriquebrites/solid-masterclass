# 3.6 — CI format and build gates — Verification

**Verdict:** PASS
**Profile:** light
**Diff range:** 0b76edf..HEAD (22e288c, 724150b, a852ef2)
**Round:** 1
**Verifier:** independent (tlc-implement Verifier agent)

## Checks

| Check | Result | Evidence |
| --- | --- | --- |
| C1 — `package.json` has `"format:check": "prettier --check ."` | PASS | Ran `node -e "if(require('./package.json').scripts['format:check']!=='prettier --check .')process.exit(1)"` at HEAD → exit 0. Confirmed in file: `package.json` scripts block contains `"format:check": "prettier --check ."`. |
| C2a — `pnpm run format:check` exits 0 at HEAD | PASS | Ran `pnpm run format:check` at HEAD → output `All matched files use Prettier code style!`, exit 0. Same run also confirms the Landing claim that format:check currently passes. |
| C2b — `pnpm run build` exits 0 at HEAD | PASS | Ran `pnpm run build` at HEAD → `tsc` completed with no output/errors, exit 0. |
| C2c — CI has "Format check" and "Build" steps running the right commands | PASS | `.github/workflows/ci.yml:53-54` — `name: Format check` / `run: pnpm run format:check`; `.github/workflows/ci.yml:56-57` — `name: Build` / `run: pnpm run build`. Both grep proofs from the checklist also matched (exit 0). |
| C2d — Type-check, Lint, Apply database migrations, Run tests keep relative order | PASS | Opened `.github/workflows/ci.yml` directly: step order is Checkout(32) → Setup pnpm(35) → Setup Node(38) → Install(44) → Type-check(47) → Lint(50) → **Format check(53)** → **Build(56)** → Apply database migrations(59) → Run tests(62). The four named steps appear in the order Type-check < Lint < Apply database migrations < Run tests, with the two new steps inserted between Lint and Apply database migrations (not disturbing the four's relative order). `awk` proof from checklist also exited 0. |
| C3 — misformatted / non-compiling change fails the corresponding CI step | PASS (manual, redone live) | (a) Temporarily reformatted `src/index.ts` line 1 to `import   {buildApp} from    "./drivers/app.js";` and ran `pnpm run format:check` → `[warn] src/index.ts` … `[ELIFECYCLE] Command failed with exit code 1.` Restored file, verified `git status --short` clean before proceeding. (b) Temporarily changed the same import to `from "./drivers/app"` (dropped `.js`) and ran `pnpm run build` → `src/index.ts(1,26): error TS2835: Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'... [ELIFECYCLE] Command failed with exit code 1.` Restored file. Final `git status --short` → clean (no output), confirming no residual working-tree changes from either exercise. |

## Landing sanity check

- `.prettierignore` exists at repo root with exactly the claimed excludes: `.agents/`, `.claude/`, `drizzle/`, `pnpm-lock.yaml` (confirmed via `cat .prettierignore`).
- `pnpm run format:check` passes at HEAD (same run used for C2a — one check, as noted in the assignment).

## Binding sources / Test policy / Faults injected

Not run — profile: light.

## Coverage

Checklist states "No enumerated set in this slice." Confirmed: this change touches only `package.json` (new script) and `.github/workflows/ci.yml` (two new steps) plus the new `.prettierignore` and a repo-wide reformat — no provider maps, status enums, or startup-config assemblies are introduced. Nothing to enumerate.

## Gate

All 6 checks (C1, C2a, C2b, C2c, C2d, C3) proven at HEAD with located evidence. Landing's `.prettierignore` claim and format:check-passes claim both confirmed. Working tree left clean after the two manual C3 exercises (`git status --short` empty).

**PASS — 6/6 checks proven.**

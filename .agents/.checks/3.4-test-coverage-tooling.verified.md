# 3.4 — Configurar medição de cobertura de testes — Verification

**Verdict**: PASS
**Profile**: light (no `## tlc-implement` profile block in AGENTS.md; default applies)
**Diff range**: 1cf249a6c4f8c66e162c8e1419565bf311d11aea..HEAD (single commit 8e9f381)
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

## Profile scope

`light` requires: proofs at HEAD, each named test/command shown to exist and run, one located
assertion per check, level and sampling gaps, and confirmation of `Swept` rows.

Skipped, per profile: step 1's binding-source contradiction/enumeration pass beyond a direct
Landing-vs-diff comparison (`light` does not require opening the source task doc as a binding
design source the way `ui` does — there is no UI/screen surface here), the `Coverage` join
(no `## Coverage` section in this artifact), `## Test policy` verdicts (no `## Test policy`
section in this artifact — its absence is the input-absent case, not a gap), and fault
injection (`standard`/`ui` only). Each is a skip, not an oversight — the checklist carries no
`Coverage` or `Test policy` section to judge under any profile.

## Landing check

Claimed: touches `package.json` (new devDependency + script), `vitest.config.ts` (`coverage`
block), `.gitignore` (ignore `coverage/`); reuses `include`/`exclude` at `vitest.config.ts:5-6`
unchanged.

Ran `git diff 1cf249a..HEAD -- .gitignore package.json vitest.config.ts` directly:
- `.gitignore`: one line added, `coverage` — matches.
- `package.json`: one devDependency (`@vitest/coverage-v8": "^5.0.1"`) and one script
  (`"test:coverage": "vitest run --coverage"`) added — matches.
- `vitest.config.ts`: only a `coverage: { provider: "v8", reporter: ["text", "lcov"],
  reportsDirectory: "coverage" }` block added after line 6; `include`/`exclude` at lines 5-6
  untouched — matches the "reused, no new file selection" claim exactly.

`pnpm-lock.yaml` also changed (108 lines) — expected fallout of adding a devDependency, not a
Landing violation.

## Checks

| Check | Claim | Proof run | Evidence | Result |
|---|---|---|---|---|
| C1 | `@vitest/coverage-v8` in `devDependencies`, version compatible with `vitest@^5.0.1` | `node -e "const pkg=require('./package.json'); const v=pkg.devDependencies['@vitest/coverage-v8']; if(!v) process.exit(1)"` — exit 0 | `package.json`: `"vitest": "^5.0.1"` and `"@vitest/coverage-v8": "^5.0.1"` — identical semver range, compatible by construction | PASS |
| C2 | `pnpm run test:coverage` exits 0 over current suite and prints a text coverage summary | `pnpm run test:coverage` — exit code `0` (confirmed via `echo $?`) | Terminal output: `Test Files 6 passed (6)`, `Tests 46 passed (46)`, then a table with header `File \| % Stmts \| % Branch \| % Funcs \| % Lines \| Uncovered Line #s` and an `All files 98.27 95.23 100 98.27` row — exact column set the check names | PASS |
| C3 | `pnpm run test:coverage` writes a machine-readable coverage artifact to `coverage/` | `test -f coverage/lcov.info` (run immediately after C2, in the same tree, `coverage/` freshly deleted beforehand) — exit code `0` | `coverage/lcov.info` present, 2300 bytes, alongside `coverage/lcov-report/`; `vitest.config.ts:9` — `reporter: ["text", "lcov"]` is what produces it, `reportsDirectory: "coverage"` (line 10) is where | PASS |

No level or sampling gaps found: each check's claimed value (a devDependency string, an exit
code plus four named columns, a file's existence) is exactly what the proof measured — none of
the three proofs settle a narrower claim than the check names.

## Swept

| Row | Check artifact says | Verified |
|---|---|---|
| observability: C2, C3 | "o próprio relatório de cobertura é a observabilidade introduzida por esta tarefa" | Holds — `vitest.config.ts:7-11` configures `reporter: ["text", "lcov"]`, and C2/C3's own proofs are the text-terminal report and the `lcov.info` file this row points at. Nothing else in the diff introduces observability, so there is no unlisted surface this row misses. |

All other `Swept` rows are `n/a` (validation, failure modes, authorization, concurrency and
ordering, data lifecycle, external-dependency failure, state transitions) or explicitly
"not in scope" per idempotency/retry reasoning — these are declared exemptions, not blanket
ones: each names why (deterministic overwrite, generated artifact with no persisted product
data), so none are findings under this step.

## Out-of-scope claim

Checklist states no coverage threshold/gate exists — `pnpm run test:coverage` never fails on
percentage. Confirmed: `vitest.config.ts`'s `coverage` block has no `thresholds` key, and the
script (`vitest run --coverage`) exited 0 with per-file coverage below 100% (`app.ts` at 95%
statements/83.33% branches) — a threshold gate would have failed this run had one existed.

## Gate

`pnpm run test:coverage` — 46 tests passed, 0 failed, exit 0; coverage report printed; `coverage/lcov.info` produced.

## Skipped (light profile)

- **Coverage join**: not run — checklist has no `## Coverage` section.
- **Test policy verdicts**: not run — checklist has no `## Test policy` section.
- **Fault injection**: not run — `standard`/`ui` only, per profile.

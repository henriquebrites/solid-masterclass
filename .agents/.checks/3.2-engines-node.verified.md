# 3.2 — Adicionar engines.node ao package.json — Verification

**Verdict**: PASS
**Profile**: light
**Diff range**: bbac3b8..HEAD
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

## Profile-gated steps

- Step 1 (binding sources / screens) - **skipped**: `ui` only, profile is `light`.
- Step 2 (run every proof) - **ran** (always).
- Step 3 (assertion check) - **ran**, core assertion-check only (always); Coverage join and
  Test policy verdicts **skipped** (`standard`/`ui` only, no such sections exist in this
  checklist).
- Step 4 (fault injection) - **skipped**: `standard`/`ui` only, profile is `light`.

## Checks

| Check | Claim | Proof run | Evidence | Result |
|---|---|---|---|---|
| C1 | `package.json` contains `"engines": { "node": ">=24" }` | `node -e "const pkg=require('./package.json'); if (pkg.engines?.node !== '>=24') process.exit(1)"` — exit 0 | `package.json:5-7` — `"engines": { "node": ">=24" }` | PASS |

Assertion targets the checklist-defined value exactly: the proof's guard compares
`pkg.engines?.node` against the literal string `'>=24'`, which is precisely the value
`">=24"` the checklist's C1 declares. Confirmed directly against the file rather than
inferred: `package.json` line 5-7 reads:

```
  "engines": {
    "node": ">=24"
  },
```

Diff confirmed: `git diff bbac3b8..HEAD` shows the single commit `ba431e5` adds exactly this
three-line block to `package.json` and nothing else changes it. No other file in the diff
range affects `engines`.

## Swept rows (existing)

Only one `Swept` row resolves to `existing`: **observability** — "`pnpm` já emite aviso
padrão quando `engines` diverge da versão ativa (sem `engine-strict`, é apenas aviso, não
bloqueio)."

Checked: no `.npmrc` file exists anywhere at the repo root (`no .npmrc`), so there is no
`engine-strict=true` or equivalent hard-enforcement setting in this repo that would
contradict the cited "warning only, not a block" behavior. pnpm's documented default
behavior (checking `engines.node` and warning, not failing, unless `engine-strict` is set)
is not contradicted by anything in the repo. This row is plausible and not contradicted.

All other `Swept` rows resolve to `n/a` and carry no claim to verify.

## Sections omitted for this profile

- **Binding sources** — not run (step 1 is `ui`-only).
- **Coverage** — not applicable; checklist declares no `## Coverage` section and the
  join is `standard`/`ui`-only.
- **Test policy rows** — not applicable; checklist declares no `## Test policy` section
  and the row-by-row verdicts are `standard`/`ui`-only.
- **Faults injected** — not run (step 4 is `standard`/`ui`-only).

## Gate

`node -e "const pkg=require('./package.json'); if (pkg.engines?.node !== '>=24') process.exit(1)"` — 1 passed, 0 failed

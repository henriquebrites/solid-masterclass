# 3.4 — Configurar medição de cobertura de testes

Sources:

- `.agents/.tasks/3.4-test-coverage-tooling.md` - settles intent, critérios, boundary, unresolved
- `package.json` - manifesto atual, sem `@vitest/coverage-v8`
- `vitest.config.ts:5-6` - `include`/`exclude` existentes, reusados sem alteração

## Out of scope

- Threshold mínimo obrigatório de cobertura (gate que falha o comando abaixo de X%) - Unresolved 1 da task fonte resolve para "sem gate", `test:coverage` nunca falha por percentual

## Landing

Toca `package.json` (nova devDependency e script), `vitest.config.ts` (bloco `coverage`) e `.gitignore` (ignorar `coverage/`). Reusa o `include`/`exclude` já existente em `vitest.config.ts:5-6` - nenhuma nova seleção de arquivos é criada.

| One-way door | Literal shape | Alternative rejected |
| --- | --- | --- |
| None - reversível (remover dependência/bloco/script desfaz a mudança por completo); nenhum threshold obrigatório é imposto por esta tarefa | | |

## Checks

### S1 - Medição de cobertura reproduzível · 3 files · ~1k

**C1** - `@vitest/coverage-v8` está em `devDependencies`, em versão compatível com `vitest@^5.0.1`
Proof: `node -e "const pkg=require('./package.json'); const v=pkg.devDependencies['@vitest/coverage-v8']; if(!v) process.exit(1)"` — exit code `0`

**C2** - `pnpm run test:coverage` conclui com exit code `0` sobre a suíte atual e imprime um resumo de cobertura em texto no terminal
Proof: `pnpm run test:coverage` — exit code `0`, saída contém a tabela de resumo do reporter `text` (colunas `% Stmts`/`% Branch`/`% Funcs`/`% Lines`)

**C3** - `pnpm run test:coverage` grava um artefato de cobertura legível por máquina em `coverage/`
Proof: após rodar `pnpm run test:coverage`, `test -f coverage/lcov.info` — exit code `0`

## Swept

- validation: n/a
- failure modes: n/a
- idempotency and retry: n/a - rodar repetidamente é determinístico, sobrescreve `coverage/`
- authorization: n/a
- concurrency and ordering: n/a
- data lifecycle: n/a - `coverage/` é artefato gerado, sem dado persistido de produto
- external-dependency failure: n/a
- state transitions: n/a
- observability: C2, C3 - o próprio relatório de cobertura é a observabilidade introduzida por esta tarefa

## Handoff

Slice único, ~1k tokens estimados de leitura — muito abaixo do orçamento de 150k. Sem handoff: um agente conclui a task inteira em um único batch.
